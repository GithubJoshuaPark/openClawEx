# OPERATIONS.md - 프로젝트 공용 운영 메모

이 문서는 OpenClaw 및 AI 어시스턴트가 Joshua님의 프로젝트를 작업할 때 공통으로 따를 운영 기준을 정의합니다.

## 1. 기본 전제

- 로컬 개발 환경에서 **Antigravity IDE**를 사용한다.
- 전역 규칙 문서 **`GEMINI.md`** 의 지침을 우선 기준으로 본다.
- 프로젝트별 규칙보다, Antigravity IDE 전역 규칙이 상위 기준이다.

## 2. 작업 기록 규칙 (`doc_fr_llm`)

다음 성격의 작업을 수행한 경우, 프로젝트 내부 `doc_fr_llm/` 폴더에 작업 기록 문서를 남긴다.

- 구조 분석
- Graft / Graphify 설정 / 실행 / 분석
- 주요 기능 구현
- 버그 수정
- 리팩토링
- 배포 / 환경 정리
- 문서 정합성 수정

### 파일명 규칙
- 형식: `YYYYMMDD_HHMMSS_작업명.md`
- **한글 파일명 준수**: 파일명에 한글이 포함되더라도 절대 URL 인코딩(`%EC%A0%84...`) 처리된 형태가 아닌 온전한 한글 UTF-8 문자로 저장한다.

### 기록 내용 기본 항목
- 작업 목적
- 확인한 내용
- 수정 / 조치 내역
- 검증 결과
- 커밋 정보
- 결론

## 3. README / ERD 정합성 규칙

다음이 바뀌면 `README.md` 와 `documents/ERD.md` 를 함께 점검한다.

- 데이터 구조
- Firestore 컬렉션 / RDB 테이블 스키마 구조
- 상태 전이
- 구현 완료 / 미완료 항목
- 주요 기능 흐름
- 운영 정책

### 원칙
- `README.md` 는 **현재 구현 상태 + 예정 항목**을 구분해 관리한다.
- `documents/ERD.md` 는 **실제 코드 기준 우선**으로 관리한다.
- 미구현 항목은 구현 완료처럼 쓰지 않는다.
- 설계 선행 항목은 반드시 `Planned`, `예정`, `진행 예정` 등으로 명확히 표기한다.

## 4. 프로젝트 해석 원칙

모든 프로젝트는 **현재 작업 중인 미완성 코드 상태만으로 해석하지 않는다.**

### 우선 기준
프로젝트를 분석하거나 구조를 설명할 때는 아래 순서를 우선한다.

1. `README.md`
2. `documents/ERD.md`
3. `graft/` 지식 그래프 (Wiring Cards)
4. 현재 소스 코드
5. Graphify 결과 (설정된 경우)

### 해석 원칙
- 현재 소스가 미완성이거나 작업 중이어도, 그 상태만을 기준으로 프로젝트 전체 구조를 단정하지 않는다.
- 문서에 정의된 **목표 구조, 개발 방향, 설계 의도**를 상위 기준으로 본다.
- 실제 코드는 그 목표 구조 대비 **현재 어디까지 구현되었는지**를 보여주는 재료로 해석한다.
- 분석 시 항상 **문서 기준 목표 상태**와 **현재 구현 상태**를 분리해서 본다.

## 5. Graft 지식 그래프 실전 활용 및 동기화 규칙 (Graft Context Rules) 🚨

프로젝트의 거시적 구조나 복잡한 의존성/호출 관계 분석 시 `graft/` 디렉토리 내 산출물을 우선 참조하여 토큰 소모를 최소화하고 안전한 코드 수정을 보장한다.

### 5.1. 사전 탐색 및 핀포인트 추출 (Selective Context)
- 프로젝트 최상위 또는 모듈별 `graft/INDEX.md` 및 마크다운 파일(Wiring Cards)을 먼저 확인하여 핵심 노드와 호출 관계를 파악하고 탐색 범위를 좁힌다.
- 파일 전체를 무작정 열람하기 전 `graft ask "<질의>"`, `graft callers <심볼>`, `graft skeleton <파일>`, `graft blast` 등의 CLI 명령어를 실행하여 필요한 맥락만 핀포인트로 추출한다.

### 5.2. 리팩토링 전 영향도(Edge) 사전 검증
- 컴포넌트, 스타일 토큰, 라우팅, 유틸리티를 수정하기 전에는 반드시 지식 그래프의 연결선(Edge) 또는 `graft callers <심볼>` 명령을 활용하여 참조하고 있는 상위/하위 컴포넌트를 사전 점검함으로써 사이드 이펙트를 원천 차단한다.

### 5.3. 지식 그래프 갱신(Update) 의무화 (Mandatory Document & Graph Sync)
- 프로젝트 내 코드 변경, 기능 추가, 삭제, 필드 병합 등 아키텍처나 기능에 변경이 발생할 경우, **반드시 `graft build` 또는 지식 그래프 갱신 스크립트를 실행하여 지식 그래프를 최신 상태로 갱신**해야 한다.

## 6. Graphify 운영 규칙 (보조)

프로젝트에 Graphify가 설정되어 있다면, 대규모 의존성 시각화 등 보조 수단으로 활용한다.

### 기본 루틴
```bash
sh scripts/update_graph.sh
```

### 활용 방식
- `graphify-out/GRAPH_REPORT.md` 먼저 확인
- 필요 시 `graphify query`, `graphify explain`, `graphify path` 사용

## 7. 작업 완료 후 기본 마감

- 관련 문서(`README.md`, `ERD.md`) 업데이트 여부 확인
- `graft build` 또는 갱신 스크립트를 통한 지식 그래프 최신화
- `doc_fr_llm/` 작업 기록 작성 (한글 파일명 준수)
- **Build-Before-Commit**: 변경 후 빌드 검증 수행
- 커밋 메시지 규칙 준수: `[feat]: ...`, `[fix]: ...`, `[docs]: ...`, `[refactor]: ...`

## 8. 운영 원칙 한 줄 요약

작업 시 기본 흐름은 아래를 따른다.

**코드 수정 → Graft 영향도 확인 → README / ERD 정합성 점검 → graft build 갱신 → doc_fr_llm 기록 → 빌드 검증 / 커밋**
